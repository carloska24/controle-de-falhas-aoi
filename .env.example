# Backend configuration
# Rename this file to .env and adjust values for your environment

# Server
PORT=3000
NODE_ENV=development

# JWT secret (REQUIRED in production)
JWT_SECRET=change-me-in-dev

# CORS: comma-separated list of allowed origins. Example:
# CORS_ORIGIN=http://127.0.0.1:5500,http://localhost:5500
CORS_ORIGIN=http://127.0.0.1:5500

# Database (optional). If not set, SQLite will be used locally.
# For PostgreSQL: postgres://user:password@localhost:5432/dbname
# DATABASE_URL=

# Enable emergency setup routes (development only)
ENABLE_EMERGENCY_ROUTES=false
